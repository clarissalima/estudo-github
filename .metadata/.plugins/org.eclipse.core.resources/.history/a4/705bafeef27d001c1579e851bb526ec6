package entities;

import java.util.List;

public class Product {

	public static boolean hasId(List<Product> list, int id) {
		Product emp = list.stream().filter(x -> x.getId() == id).findFirst().orElse(null);
		return emp != null;
	}
}		
}
